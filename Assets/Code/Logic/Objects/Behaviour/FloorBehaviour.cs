using Static;

public class FloorBehaviour : BuildingBehaviour
{
  // ===================================================================================================
  public FloorBehaviour(View view, BuildData buildData) : base(view, buildData) { }
}